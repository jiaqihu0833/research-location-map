---
name: research-location-map
description: 为地理学、城乡规划学论文制作可复现的研究区区位图，支持公开数据、用户空间数据及混合数据，组织两级或三级定位图、区域主图和细节图。适用于行政区位、地形水系、交通与样点图，包含坐标核验、出版布局和来源记录；不用于空间统计推断、选址评价或生成式地图插画。
---

# 科研区位图

把真实空间数据转为地理位置准确、层级清楚、可复现的论文图件。先确定图件要表达的位置关系，再选尺度、数据与版式。研究区边界不必等于行政边界。

## 使用边界与权限

- 第一版处理行政区位、地形水系、交通与样点三类图。可叠加已有分析结果，但不把区位图描述为新完成的可达性、热点或因果分析。
- 读取用户提供的资料并保留原件。使用新的工作目录存放配置、衍生数据和输出，不覆盖数据、工程或论文。
- 读取当前会话权限。运行程序、安装依赖、下载数据、调用服务或上传文件，须已有相应授权；已有授权不重复询问。若尚无授权，先完成独立的设计和代码工作，再列明具体动作、用途与影响。技能被选中本身不等于获得这些权限。
- 不读取密钥或改变环境配置以绕过缺失能力。没有联网授权时也不得触发 Cartopy、PROJ、QGIS 在线底图等隐式下载。
- 真实边界和坐标必须来自空间数据。生成式图片不能代替地图；测试用合成数据必须获授权、明确标记，不能冒充研究结果。

## 1. 判断输入与环境

优先使用对话中已有的信息，集中询问影响正确性的缺口。至少记录：研究区名称及范围定义、数据模式、数据年份、图型、输出语言、两级/三级布局、图幅尺寸和格式。字体和颜色等低风险选择可先提出默认方案。

| 情况 | 下一步 |
| --- | --- |
| 仅有地名 | 确定同名地的上级行政区和研究范围语义，列数据获取计划；没有几何数据时不输出貌似真实的成图 |
| 已有边界、样点 | 检查格式、字段、CRS、时点、几何及范围；坐标来源不明则先解决 |
| 混合数据 | 优先使用用户确认的研究区，逐层核对背景与样本的年份、坐标和精度 |

只读查看可用工具、应用目录、依赖元数据，区分“文件存在”“可导入”“可运行”“已通过绘图测试”。执行导入检查也属于运行代码，遵守本次权限。

- 默认静态路线：GeoPandas、Cartopy、Matplotlib、pyproj；DEM 才需要 Rasterio，Excel 需要适用的表格读取引擎。
- 可编辑路线：QGIS/PyQGIS。MCP 可用时可驱动现有工具；没有 MCP 不代表没有 QGIS，也不强制安装 MCP。使用可用的本地 PyQGIS 路线同样须获得执行授权。
- 需要工程交付时读 [QGIS 工作流](references/qgis-workflow.md)。脚本不自动生成 `.qgz`，不得把静态图冒充可编辑工程。

## 2. 准备数据

按需阅读 [数据来源与接入](references/data-sources.md)。三种模式最终都应形成可追溯的本地快照：

- `public`：经过来源和尺度审查、获准获取的公开数据。
- `user`：用户提供的空间图层或坐标表。
- `mixed`：公开背景与用户研究区、样点等组合。

支持 GeoPackage、GeoJSON、完整 Shapefile ZIP、GeoTIFF、CSV/Excel。原始文件不改动；必要的清洗、重投影、筛选应保留规则与衍生文件。Shapefile 的几何、属性、索引和投影文件要配套，编码也应核实。

每层记录来源、版本、数据年份、许可、空间精度/分辨率、获取日期。无法查明的非关键信息写明 `unknown` 及影响；关键坐标、来源可靠性、用途许可或边界时点无法满足任务时，暂停相关图层交付。不能以“最新”代替具体年份。

## 3. 地理核查

按 [坐标、投影与边界规则](references/crs-and-boundaries.md) 检查：

1. 先读 CRS 元数据、数据说明和坐标来源；数值范围只能发现疑点，不能自动断定 CRS 或交换经纬度列。
2. 核查 WGS84、CGCS2000 与互联网地图坐标来源。GCJ-02/BD-09 不能通过修改 CRS 标签解决；附带脚本拒绝直接处理此类未转换数据。
3. 投影按范围和用途选择。Web Mercator 不是论文通用投影；全国、省域、局部图可使用不同投影。
4. 区分行政边界、调查边界、规划范围、历史边界及现行边界。中国相关图件核对适用的标准地图与出版要求，不保证任意公开边界可直接用于投稿。
5. 检查无效几何、空对象、样点位置、图层对齐和数据覆盖。异常先报告，不静默删除、修复或“吸附”到边界。

## 4. 设计与配置

读取 [布局规范](references/layouts.md) 和 [配置契约](references/configuration.md)。从 [请求模板](assets/request-template.json) 复制到任务目录，填入真实数据与范围；模板中的 `null` 是未决输入，不是可直接出图的示例。

- 行政区位：强调研究区与上级地域关系，背景层级应清楚。
- 地形水系：只用真实 DEM 与水系；说明高程单位、年份和分辨率，不把渲染地形当作分析结果。
- 交通样点：道路分级适度，样点与研究范围突出；图面仅说明位置，不把直线距离解读为通行距离。
- 两级或三级地图框根据研究问题选择。定位框应来自目标地图框最终可见范围，不能任意画矩形冒充对应范围。
- 比例尺绑定相应地图框。指北针表示其放置位置的局部真北；可省略不需要的指北针，避免装饰性指向。

## 5. 绘图与保留复现材料

获得执行授权、检查依赖和配置后，静态路线可使用 `scripts/render_location_map.py`。入口形式：

```text
python /absolute/path/to/research-location-map/scripts/render_location_map.py /absolute/path/to/request.json
```

解释器选择已确认可用的环境；不要照抄未验证的路径。程序的范围是本地文件的装载、检查和制图，**不负责下载公开数据或地名解析**。数据获取由前面的工作流完成。

第一版脚本限两个或三个、不跨日期变更线的地图框，使用已登记的投影 EPSG 坐标系，拒绝 EPSG:3857；这是这个辅助程序的实现边界，不是普遍制图规则。超出其能力时使用经过验证的定制脚本或 QGIS 路线，明确说明额外验证范围。

导出 PNG、SVG、PDF 中用户所需格式。包含栅格地形时，SVG/PDF 也会包含栅格部分，不宣称整幅均为矢量。输出配置副本、来源/输入校验摘要、软件版本和检查报告。可编辑路线另保留 `.qgz`、关联数据与样式，检查移动目录后的相对路径。

## 6. 验证与交付

按 [质量检查和验收场景](references/quality-and-acceptance.md) 完成数据检查、成图尺寸下的目视检查和导出复核。

状态必须准确区分：

- `static_review_only`：只阅读了文件和代码。
- `rendered_needs_visual_review`：已运行出图，仍需核对图面。
- `accepted`：规定检查实际完成，并有记录支持。

程序运行成功不是出版质量通过。检查文字缺字、标注重叠、图例语义、比例尺、定位框、数据署名和导出一致性。不能证明的检查写“未验证”，不能写“通过”。

最后交付：图件和工程路径（如适用）、数据与参数记录、执行和检查状态、仍存在的限制。环境缺失时交付可复用的配置、代码和最小后续步骤，不能伪造地图或声称实际测试成功。
